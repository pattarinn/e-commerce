<% stylesheet_link_tag 'edit_product.css' %>

<h1>Editing Product</h1>

<%= render 'form', product: @product %>

<div class="menu">
  <div class="button">
    <%= link_to 'Back', product_path, class: "btn btn-info" %>
  </div>
</div>